# nestjs-arque-module

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nestjs-arque-module` to build the library.

## Running unit tests

Run `nx test nestjs-arque-module` to execute the unit tests via [Jest](https://jestjs.io).
